# House Wallet (TESTNET ONLY - Keep secure!)
HOUSE_PRIVATE_KEY=cVxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
HOUSE_ADDRESS=mnxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Database
DATABASE_URL=sqlite:///./dice_game.db

# Game Configuration
HOUSE_EDGE=0.02
MIN_BET_SATOSHIS=600          # ~$0.30 at $50k BTC price
MAX_BET_SATOSHIS=1000000
MIN_MULTIPLIER=1.1
MAX_MULTIPLIER=98.0

# Bitcoin Network
CONFIRMATIONS_REQUIRED=1
NETWORK=testnet
TX_DETECTION_TIMEOUT_MINUTES=60

# Transaction Fees
DEFAULT_TX_FEE_SATOSHIS=250        # Estimated fee for typical transaction
FEE_BUFFER_SATOSHIS=1000           # Buffer for UTXO selection
DUST_LIMIT_SATOSHIS=546            # Bitcoin dust limit (don't change)

# API Endpoints
# Mempool.space - Primary API for transaction detection and broadcasting
MEMPOOL_SPACE_API=https://mempool.space/testnet/api
MEMPOOL_WEBSOCKET_URL=wss://mempool.space/testnet/api/v1/ws

# Blockstream - Backup API for broadcasting
BLOCKSTREAM_API=https://blockstream.info/testnet/api

# API Request Timeouts (seconds)
API_REQUEST_TIMEOUT=10             # Timeout for API requests
BROADCAST_TIMEOUT=15               # Timeout for transaction broadcasts

# WebSocket Configuration
WS_PING_INTERVAL=30                # WebSocket ping interval (seconds)
WS_PING_TIMEOUT=20                 # WebSocket ping timeout (seconds)
WS_RECONNECT_DELAY=5               # Initial reconnect delay (seconds)
WS_MAX_RECONNECT_DELAY=60          # Maximum reconnect delay (seconds)

# Security
SECRET_KEY=your-secret-key-change-this-in-production

# Server
HOST=0.0.0.0
PORT=8001

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000
