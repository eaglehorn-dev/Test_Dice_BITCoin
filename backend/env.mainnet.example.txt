# ⚠️⚠️⚠️ MAINNET CONFIGURATION - REAL BITCOIN! ⚠️⚠️⚠️
# 
# READ MAINNET_SETUP_GUIDE.md BEFORE USING THIS!
#
# This file is for PRODUCTION use with REAL BITCOIN
# Mistakes here can result in REAL FINANCIAL LOSS
# Test EVERYTHING on testnet first!

# House Wallet (⚠️ MAINNET - EXTREMELY SENSITIVE!)
# NEVER commit the actual .env file with real keys!
# Use a dedicated wallet with MINIMAL funds (start with 0.001 BTC)
# Format: WIF private key (starts with 5, K, or L)
HOUSE_PRIVATE_KEY=your_mainnet_private_key_WIF_format_here
HOUSE_ADDRESS=your_mainnet_bitcoin_address_here

# Database (use separate database for mainnet!)
DATABASE_URL=sqlite:///./dice_game_mainnet.db

# Game Configuration (CONSERVATIVE settings for mainnet!)
HOUSE_EDGE=0.02
MIN_BET_SATOSHIS=600            # ~$0.30 at $50k BTC price
MAX_BET_SATOSHIS=100000         # 0.001 BTC (~$50 USD) - START LOW!
MIN_MULTIPLIER=1.1
MAX_MULTIPLIER=10.0             # Lower maximum for safety

# Bitcoin Network (MAINNET)
CONFIRMATIONS_REQUIRED=3        # ⚠️ Require MORE confirmations on mainnet!
NETWORK=mainnet
TX_DETECTION_TIMEOUT_MINUTES=120

# API Endpoints (Mempool.space used for transaction detection via WebSocket)
BLOCKSTREAM_API=https://blockstream.info/api
MEMPOOL_SPACE_API=https://mempool.space/api

# Security (⚠️ MUST CHANGE THESE!)
SECRET_KEY=CHANGE-THIS-TO-SECURE-RANDOM-STRING-MIN-32-CHARACTERS
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Rate Limiting (important for mainnet!)
MAX_REQUESTS_PER_MINUTE=30
MAX_BETS_PER_USER_PER_HOUR=50

# Server
HOST=0.0.0.0
PORT=8001
RELOAD=false                    # Never use reload in production!

# Frontend URL (use your production domain with HTTPS)
FRONTEND_URL=https://your-production-domain.com

# Monitoring
ENABLE_LOGGING=true
LOG_LEVEL=INFO
LOG_FILE=dice_game_mainnet.log

# ⚠️ SECURITY REMINDERS:
# 1. This file (.env) should NEVER be committed to git
# 2. Keep your private key extremely secure
# 3. Use strong SECRET_KEY (generate with: openssl rand -hex 32)
# 4. Start with TINY amounts (0.001 BTC)
# 5. Monitor EVERY transaction closely
# 6. Have cold storage ready for excess funds
# 7. Set up alerts for unusual activity
# 8. Consult legal counsel about gambling laws
# 9. Test EVERYTHING on testnet first!
# 10. You are responsible for all losses!
